f_number= input("Enter first Number:")
s_number=input("Enter second Number:")
result=float(f_number)+float(s_number) # when getting an input from the user it get as string if we use int it get only integer numbers if we use  float we can get the decimal points also
print(result)